{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dmoffre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dmoffre.id }}</td>
            </tr>
            <tr>
                <th>Idoffer</th>
                <td>{{ dmoffre.idoffer }}</td>
            </tr>
            <tr>
                <th>Iduser</th>
                <td>{{ dmoffre.iduser }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dmoffre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dmoffre_edit', { 'id': dmoffre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
